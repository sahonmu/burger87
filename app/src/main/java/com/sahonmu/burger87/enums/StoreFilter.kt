package com.sahonmu.burger87.enums

enum class StoreSort {
    BASIC,
    SORT,
    SORT_DESC,
}

enum class ScoreSort {
    SORT,
    SORT_DESC
}

enum class ClosedFilter {
    ALL,
    OPERATION,
    CLOSED
}